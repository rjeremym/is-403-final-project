<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Idea - Business Ideas Tracker</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('partials/nav') %>

    <div class="container">
        <header class="page-header">
            <h1>Edit Business Idea</h1>
            <a href="/ideas" class="btn btn-secondary">Back to Ideas</a>
        </header>

        <div class="form-card">
            <% if (error_message) { %>
                <div class="alert alert-error">
                    <%= error_message %>
                </div>
            <% } %>

            <form action="/editIdea/<%= idea.ideaid %>" method="POST">
                <div class="form-group">
                    <label for="ideaname">Idea Name *</label>
                    <input type="text" id="ideaname" name="ideaname" required 
                           value="<%= idea.ideaname %>">
                </div>

                <div class="form-group">
                    <label for="ideadescription">Description *</label>
                    <textarea id="ideadescription" name="ideadescription" rows="4" required><%= idea.ideadescription %></textarea>
                </div>

                <div class="form-group">
                    <label for="targetcustomer">Target Customer</label>
                    <input type="text" id="targetcustomer" name="targetcustomer" 
                           value="<%= idea.targetcustomer || '' %>">
                </div>

                <div class="form-group">
                    <label for="marketingstrategy">Marketing Strategy</label>
                    <textarea id="marketingstrategy" name="marketingstrategy" rows="3"><%= idea.marketingstrategy || '' %></textarea>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="estimatedcost">Estimated Cost ($)</label>
                        <input type="number" id="estimatedcost" name="estimatedcost" 
                               step="0.01" min="0" value="<%= idea.estimatedcost || '' %>">
                    </div>

                    <div class="form-group">
                        <label for="timeline">Timeline to Implement</label>
                        <input type="text" id="timeline" name="timeline" 
                               value="<%= idea.timeline || '' %>">
                    </div>
                </div>

                <div class="form-group">
                    <label for="potential">Success Potential (1-5 stars)</label>
                    <select id="potential" name="potential">
                        <option value="">Select rating...</option>
                        <option value="1" <%= idea.potential == 1 ? 'selected' : '' %>>★☆☆☆☆ - Low potential</option>
                        <option value="2" <%= idea.potential == 2 ? 'selected' : '' %>>★★☆☆☆ - Below average</option>
                        <option value="3" <%= idea.potential == 3 ? 'selected' : '' %>>★★★☆☆ - Average</option>
                        <option value="4" <%= idea.potential == 4 ? 'selected' : '' %>>★★★★☆ - High potential</option>
                        <option value="5" <%= idea.potential == 5 ? 'selected' : '' %>>★★★★★ - Excellent potential</option>
                    </select>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Update Idea</button>
                    <a href="/ideas" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>

        <!-- Collaborators Section (only visible to owner) -->
        <% if (isOwner) { %>
            <div class="collaborators-section">
                <h2>Manage Collaborators</h2>
                
                <% if (collaborators.length > 0) { %>
                    <div class="current-collaborators">
                        <h3>Current Collaborators</h3>
                        <ul class="collaborator-list">
                            <% collaborators.forEach(collab => { %>
                                <li class="collaborator-item">
                                    <span class="collaborator-name">
                                        <%= collab.firstname %> <%= collab.lastname %> 
                                        (<%= collab.username %>)
                                    </span>
                                    <form action="/removeCollaborator/<%= collab.collaborationid %>" 
                                          method="POST" style="display: inline;"
                                          onsubmit="return confirm('Remove this collaborator?');">
                                        <button type="submit" class="btn btn-small btn-danger">Remove</button>
                                    </form>
                                </li>
                            <% }) %>
                        </ul>
                    </div>
                <% } %>

                <% if (availableUsers.length > 0) { %>
                    <div class="add-collaborator">
                        <h3>Add New Collaborator</h3>
                        <form action="/addCollaborator/<%= idea.ideaid %>" method="POST" class="inline-form">
                            <select name="collaboratorId" required class="collaborator-select">
                                <option value="">Select a user...</option>
                                <% availableUsers.forEach(user => { %>
                                    <option value="<%= user.userid %>">
                                        <%= user.firstname %> <%= user.lastname %> (<%= user.username %>)
                                    </option>
                                <% }) %>
                            </select>
                            <button type="submit" class="btn btn-primary">Add Collaborator</button>
                        </form>
                    </div>
                <% } else if (collaborators.length === 0) { %>
                    <p class="info-text">No other users available to add as collaborators.</p>
                <% } %>
            </div>
        <% } else { %>
            <div class="collaborators-section">
                <div class="alert alert-info">
                    You are a collaborator on this idea. Only the owner can manage collaborators.
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>